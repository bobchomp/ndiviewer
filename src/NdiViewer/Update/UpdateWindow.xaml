<Window x:Class="NdiViewer.Update.UpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Update Available"
        Width="440" SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Icon="/Assets/app.ico"
        Topmost="True"
        Closing="Window_Closing">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="MessageText" FontSize="14" TextWrapping="Wrap"
                   Text="A newer version of NDI Viewer is available. Please update now."/>

        <TextBlock Grid.Row="1" x:Name="StatusText" Margin="0,10,0,0" Foreground="Gray"
                   TextWrapping="Wrap" Visibility="Collapsed"/>

        <ProgressBar Grid.Row="2" x:Name="ProgressBarControl" Margin="0,10,0,0" Height="18"
                     Minimum="0" Maximum="100" Visibility="Collapsed"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button x:Name="UpdateButton" Content="Update" Padding="24,6" IsDefault="True" Click="UpdateButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
